# Optional: override the browser → server WebSocket URL.
# By default the app uses the same origin (ws://host/ssh or wss://host/ssh),
# which is what you want in every normal deployment.
# Only set this when developing the frontend against a separately running backend.
# VITE_WS_URL=ws://localhost:3000/ssh
